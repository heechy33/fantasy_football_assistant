/** Back-compat alias — the body moved to `/leagues/connect`'s shared component (exactly one
 * connect surface); the onboarding wizard step renders the same thing under its own route. */
export { ConnectLeagueRoute as OnboardingLeague } from '../ConnectLeagueRoute';
