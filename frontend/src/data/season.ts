/** The fantasy season the app is built for. One constant because both the connect surface
 * (listing a Sleeper account's leagues) and the Draft Room launcher (listing its drafts) must
 * agree on it — a literal in each would drift apart across the offseason rollover. */
export const CURRENT_SEASON = '2026';
